# Phase 1 QR Correction

- An now uses `images/zalo-an-qr.jpg`.
- Teacher George now uses `images/zalo-an-qr.png`.
- Removed the incorrect blue framed Teacher George QR image from the release.
- Removed the duplicate `an-zalo-qr.jpg` file to prevent future confusion.
- The two source files intentionally share the same base name and differ only by extension, exactly as supplied.
